{
  "version": 1,
  "updated": "2026-07-09",
  "_docs": "Single source of truth for every bot. The orchestrator (/api/orchestrator) reads this, resolves dependsOn order, and runs each enabled bot with its timeout. kind: 'http' calls an existing /api endpoint; 'grok' is a config-only Grok bot run by bots/lib/grokbot.js (prompt file + inputs + output blob, no code needed); 'internal' maps to a module in bots/lib. enabled can be overridden at runtime from ops.html (stored in the tower-bot-overrides blob, since serverless functions cannot edit this file). alert.onFail / alert.qualityDrop feed the alerts bot.",
  "bots": [
    {
      "id": "briefing",
      "name": "Daily Briefing",
      "purpose": "Surface the 5 most important Texas Longhorns news items of the last 48h for the homepage briefing panel",
      "kind": "http",
      "endpoint": "/api/briefing?cron=1",
      "method": "GET",
      "schedule": "orchestrator am+pm (06:00 / 18:00 UTC)",
      "promptFile": "bots/prompts/briefing.md",
      "inputs": ["web-search", "data/facts.json"],
      "outputs": ["blob:tower-briefing.json"],
      "outputType": "briefing",
      "dependsOn": [],
      "enabled": true,
      "timeoutMs": 88000,
      "alert": { "onFail": true, "qualityDrop": 15 }
    },
    {
      "id": "story-generator",
      "name": "Newsroom Story Generator",
      "legacyName": "generate-story",
      "purpose": "Write one deep-dive draft story per run for the newsroom CMS (saved as draft, never auto-published)",
      "kind": "http",
      "endpoint": "/api/generate-story",
      "method": "POST",
      "schedule": "orchestrator am+pm (06:00 / 18:00 UTC)",
      "promptFile": "bots/prompts/story-generator.md",
      "inputs": ["web-search", "data/facts.json"],
      "outputs": ["blob:tower-stories.json (via /api/stories-db, status=draft)"],
      "outputType": "story",
      "dependsOn": [],
      "enabled": true,
      "timeoutMs": 140000,
      "alert": { "onFail": true, "qualityDrop": 15 }
    },
    {
      "id": "stories-refresh",
      "name": "Stories Feed Refresh",
      "purpose": "Generate 3 grounded stories per run, dedupe against the archive, publish to the public stories feed",
      "kind": "http",
      "endpoint": "/api/stories-refresh",
      "method": "GET",
      "schedule": "orchestrator am+pm (06:00 / 18:00 UTC)",
      "promptFile": "bots/prompts/stories-refresh.md",
      "inputs": ["web-search", "data/facts.json", "data/roster.json", "data/db.json", "data/depth-chart.json"],
      "outputs": ["blob:tower-ai-stories.json", "blob:tower-bot-rejected.json"],
      "outputType": "stories",
      "dependsOn": [],
      "enabled": true,
      "timeoutMs": 190000,
      "alert": { "onFail": true, "qualityDrop": 15 }
    },
    {
      "id": "verify-recruiting",
      "name": "Recruiting Fact-Checker",
      "purpose": "Verify the live 2027 class ranks / commit count via web search; feeds the labeled 'Live via Grok' block on recruiting.html",
      "kind": "http",
      "endpoint": "/api/verify-recruiting?refresh=1",
      "method": "GET",
      "schedule": "orchestrator am+pm (06:00 / 18:00 UTC)",
      "promptFile": "bots/prompts/verify-recruiting.md",
      "inputs": ["web-search", "data/facts.json", "data/recruiting.json"],
      "outputs": ["blob:tower-recruiting-live.json"],
      "outputType": "recruiting-live",
      "dependsOn": [],
      "enabled": true,
      "timeoutMs": 88000,
      "alert": { "onFail": true, "qualityDrop": 20 }
    },
    {
      "id": "odds",
      "name": "Odds Cache Warm",
      "purpose": "Warm the odds edge cache for the 2026 schedule (The Odds API or consensus fallback)",
      "kind": "http",
      "endpoint": "/api/odds?cron=1",
      "method": "GET",
      "schedule": "orchestrator am+pm (06:00 / 18:00 UTC)",
      "promptFile": null,
      "inputs": ["The Odds API"],
      "outputs": ["edge cache"],
      "outputType": null,
      "dependsOn": [],
      "enabled": true,
      "timeoutMs": 20000,
      "alert": { "onFail": true }
    },
    {
      "id": "social-drafter",
      "name": "Social Drafter",
      "purpose": "Turn the day's top story into 3 X post drafts for human review on ops.html — never auto-posted",
      "kind": "grok",
      "schedule": "orchestrator am+pm, after stories-refresh",
      "promptFile": "bots/prompts/social-drafter.md",
      "webSearch": false,
      "inputs": ["topStory", "data/facts.json"],
      "outputs": ["blob:tower-social-drafts.json"],
      "outputType": "social",
      "dependsOn": ["stories-refresh"],
      "enabled": true,
      "timeoutMs": 60000,
      "alert": { "onFail": true, "qualityDrop": 15 }
    },
    {
      "id": "alerts",
      "name": "Ops Alerts",
      "purpose": "Runs last every cycle; posts to Slack (SLACK_OPS_WEBHOOK) on bot failure, quality drop >15, missed runs, or review queue >10",
      "kind": "internal",
      "module": "alerts",
      "schedule": "orchestrator am+pm, always last; also the 10:05 UTC watchdog (GitHub Actions cron)",
      "promptFile": null,
      "inputs": ["run results", "blob:tower-bot-stats.json", "blob:tower-bot-rejected.json", "NEEDS-VERIFICATION.md"],
      "outputs": ["Slack webhook"],
      "outputType": null,
      "dependsOn": ["*"],
      "enabled": true,
      "timeoutMs": 15000,
      "alert": null
    }
  ]
}
